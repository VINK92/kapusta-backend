version: 1
buildCommand: yarn ci && yarn run build && nest build
type: yaml
services:
  - name: kapusta-beckend
    env: node
    nodeVersion: 14.20.1
    startCommand: start:prod
    github:
      autoDeploy: true
      branch: main
