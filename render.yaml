version: 1
buildCommand: yarn && nest build
services:
  - name: kapusta-beckend
    type: web
    env: node
    nodeVersion: 14.20.1
    startCommand: start:prod
    github:
      autoDeploy: true
      branch: main
