version: 1
services:
  - name: kapusta-beckend
    type: web
    env: node
    startCommand: yarn && start:prod


resources:
  repositories:
    - repository: templates
      type: github
      autoDeploy: true
      branch: master