version: 1
services:
  - name: kapusta-beckend
    type: web
    env: node
    startCommand: yarn && start:prod
    rootDir: src
    autoDeploy: true
    branch: master
    buildFilter:
    paths:
      - src/**/*.ts
    ignoredPaths:
      - src/**/*.test.ts

resources:
  